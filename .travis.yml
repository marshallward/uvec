language: c
before_install:
    - wget https://github.com/libcheck/check/files/71408/check-0.10.0.tar.gz
    - tar -xvf check-0.10.0.tar.gz
    - cd check-0.10.0
    - ./configure
    - make
    - export LD_LIBRARY_PATH=$(pwd)/check-0.10.0/src/.libs:$LD_LIBRARY_PATH
    - cd ..
